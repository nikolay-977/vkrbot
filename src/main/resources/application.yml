server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/admin_db_dev
    username: admin_user_dev
    password: admin_password_dev
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML

  mail:
    host: smtp.yandex.ru
    port: 465
    username: ${EMAIL_USER:klnkstn@yandex.ru}
    password: ${EMAIL_PASSWORD:csdqrpuxpmdwvzim}
    protocol: smtps
    default-encoding: UTF-8
    properties:
      mail:
        debug: true

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:8496024386:AAHGbjagwhBjcUxZP0k2vsmIYA7LU-GCRik}
    username: ${TELEGRAM_BOT_USERNAME:SF_VKR_bot}

logging:
  level:
    org.springframework.security: DEBUG
    com.admin.app: DEBUG